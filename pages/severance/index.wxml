<view class="severance-detail-page">

  <view class="severance-head">

    <view class="severance-info">

      <view class="severance-title">
        <view class="severance-name">{{content.title}}</view>
        <view class="severance-tag">
          <t-button open-type="share" t-class="shareBtn">
            <view class="btn-icon">
              <t-icon name="share" size="32rpx" />
              <view class="share-text">分享</view>
            </view>
          </t-button>
        </view>
      </view>

    </view>

    <t-image src="{{content.image}}" t-class="swipe-img t-swiper" />

  </view>

  <view class="severance-body">

    <view class="comments-wrap">
      <view class="comments-head">
        <view class="comments-title-wrap">
          {{content.survey.title}}
        </view>
      </view>
    </view>

    <!-- ------ Q01 ------ -->
    <view class="comments-wrap-q">
      <view class="comments-head-q">
        <view class="comments-title-wrap-q">
          {{content.survey.q1}}
        </view>
        <view class="comment-item-content-q">

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q01A01 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q01A01 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ1A1"
            />
            <view class="answer-check-item-text-q">
              正在医疗期间
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q01A02 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q01A02 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ1A2"
            />
            <view class="answer-check-item-text-q">
              在本单位患有职业病，并丧失或部分丧失劳动能力
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q01A03 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q01A03 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ1A3"
            />
            <view class="answer-check-item-text-q">
              从事接触职业病危害作业的劳动者未进行离岗前职业健康检查，或者疑似职业病病人在诊断或者医学观察期间的
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q01A04 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q01A04 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ1A4"
            />
            <view class="answer-check-item-text-q">
              否
            </view>
          </view>

        </view>
      </view>
    </view>

    <!-- ------ Q02 ------ -->
    <view class="comments-wrap-q">
      <view class="comments-head-q">
        <view class="comments-title-wrap-q">
          {{content.survey.q2}}
        </view>
        <view class="comment-item-content-q">

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q02A01 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q02A01 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ2A1"
            />
            <view class="answer-check-item-text-q">
              正在医疗期间
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q02A02 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q02A02 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ2A2"
            />
            <view class="answer-check-item-text-q">
              在本单位工伤，并丧失或部分丧失劳动力
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q02A03 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q02A03 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ2A3"
            />
            <view class="answer-check-item-text-q">
              否
            </view>
          </view>

        </view>
      </view>
    </view>

    <!-- ------ Q03 ------ -->
    <view class="comments-wrap-q">
      <view class="comments-head-q">
        <view class="comments-title-wrap-q">
          {{content.survey.q3}}
        </view>
        <view class="comment-item-content-q">

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q03A01 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q03A01 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ3A1"
            />
            <view class="answer-check-item-text-q">
              是
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q03A02 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q03A02 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ3A2"
            />
            <view class="answer-check-item-text-q">
              否
            </view>
          </view>

        </view>
      </view>
    </view>

    <!-- ------ Q04 ------ -->
    <view class="comments-wrap-q">
      <view class="comments-head-q">
        <view class="comments-title-wrap-q">
          {{content.survey.q4}}
        </view>
        <view class="comment-item-content-q">

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q04A01 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q04A01 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ4A1"
            />
            <view class="answer-check-item-text-q">
              破产、解散
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q04A02 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q04A02 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ4A2"
            />
            <view class="answer-check-item-text-q">
              经济性裁员
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q04A03 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q04A03 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ4A3"
            />
            <view class="answer-check-item-text-q">
              否
            </view>
          </view>

        </view>
      </view>
    </view>

    <!-- ------ Q05 ------ -->
    <view class="comments-wrap-q">
      <view class="comments-head-q">
        <view class="comments-title-wrap-q">
          {{content.survey.q5}}
        </view>
        <view class="comment-item-content-q">

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q05A01 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q05A01 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ5A1"
            />
            <view class="answer-check-item-text-q">
              是
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q05A02 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q05A02 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ5A2"
            />
            <view class="answer-check-item-text-q">
              否
            </view>
          </view>

        </view>
      </view>
    </view>

    <!-- ------ Q06 ------ -->
    <view class="comments-wrap-q">
      <view class="comments-head-q">
        <view class="comments-title-wrap-q">
          {{content.survey.q6}}
        </view>
        <view class="comment-item-content-q">
          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q06A01 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q06A01 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ6A1"
            />
            <view class="answer-check-item-text-q">
              是
            </view>
          </view>
          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q06A02 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q06A02 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ6A2"
            />
            <view class="answer-check-item-text-q">
              否
            </view>
          </view>
        </view>
      </view>
    </view>

    <!-- ------ Q07 ------ -->
    <view class="comments-wrap-q">
      <view class="comments-head-q">
        <view class="comments-title-wrap-q">
          {{content.survey.q7}}
        </view>
        <view class="comment-item-content-q">

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q07A01 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q07A01 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ7A1"
            />
            <view class="answer-check-item-text-q">
              用人单位提前30日以书面形式通知本人
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q07A02 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q07A02 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ7A2"
            />
            <view class="answer-check-item-text-q">
              用人单位没有提前30日以书面形式通知本人
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q07A03 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q07A03 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ7A3"
            />
            <view class="answer-check-item-text-q">
              否
            </view>
          </view>

        </view>
      </view>
    </view>

    <!-- ------ Q08 ------ -->
    <view class="comments-wrap-q">
      <view class="comments-head-q">
        <view class="comments-title-wrap-q">
          {{content.survey.q8}}
        </view>
        <view class="comment-item-content-q">

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q08A01 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q08A01 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ8A1"
            />
            <view class="answer-check-item-text-q">
              用人单位提前30日以书面形式通知劳动者本人
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q08A02 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q08A02 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ8A2"
            />
            <view class="answer-check-item-text-q">
              用人单位没有提前30日以书面形式通知劳动者本人
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q08A03 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q08A03 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ8A3"
            />
            <view class="answer-check-item-text-q">
              否
            </view>
          </view>

        </view>
      </view>
    </view>

    <!-- ------ Q09 ------ -->
    <view class="comments-wrap-q">
      <view class="comments-head-q">
        <view class="comments-title-wrap-q">
          {{content.survey.q9}}
        </view>
        <view class="comment-item-content-q">

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q09A01 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q09A01 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ9A1"
            />
            <view class="answer-check-item-text-q">
              用人单位提前30日以书面形式通知劳动者本人
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q09A02 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q09A02 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ9A2"
            />
            <view class="answer-check-item-text-q">
              用人单位没有提前30日以书面形式通知劳动者本人。
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q09A03 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q09A03 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ9A3"
            />
            <view class="answer-check-item-text-q">
              否
            </view>
          </view>

        </view>
      </view>
    </view>

    <!-- ------ Q10 ------ -->
    <view class="comments-wrap-q">
      <view class="comments-head-q">
        <view class="comments-title-wrap-q">
          {{content.survey.q10}}
        </view>
        <view class="comment-item-content-q">

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q10A01 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q10A01 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ10A1"
            />
            <view class="answer-check-item-text-q">
              是
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q10A02 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q10A02 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ10A2"
            />
            <view class="answer-check-item-text-q">
              否
            </view>
          </view>

        </view>
      </view>
    </view>

    <!-- ------ Q11 ------ -->
    <view class="comments-wrap-q">
      <view class="comments-head-q">
        <view class="comments-title-wrap-q">
          {{content.survey.q11}}
        </view>
        <view class="comment-item-content-q">

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q11A01 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q11A01 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ11A1"
            />
            <view class="answer-check-item-text-q">
              是
            </view>
          </view>

          <view class="answer-check-item-q">
            <t-icon
              size="40rpx"
              color="{{store.q11A02 ? '#FA4126' : '#BBBBBB'}}"
              name="{{store.q11A02 ? 'check-circle-filled' : 'circle'}}"
              class="answer-check-q"
              bindtap="selectAnswerQ11A2"
            />
            <view class="answer-check-item-text-q">
              否
            </view>
          </view>

        </view>
      </view>
    </view>

    <!-- ------ Compute ------ -->
    <view class="comments-wrap">
      <view class="comments-head">
        <view class="comments-title-wrap">
        </view>
        <view class="comment-item-content">
        </view>
        <view class="submit-btn" bindtap="reset">
          重新计算
        </view>
      </view>
    </view>

    <!-- ------ Result ------ -->
    <view class="comments-wrap">
      <view class="comments-head" bindtap="navToCommentsListPage">
        <view class="comments-title-wrap">
        </view>
      </view>
      <view class="comment-item-wrap">
        <view class="comment-item-content">
          <rich-text nodes="{{compensation}}"></rich-text>
        </view>
      </view>
    </view>

  </view>

</view>
